# API base URL (include /api). Required for products, branches, checkout, and admin.
# Copy to .env.local and restart the web dev server.
# With API running on port 4000: npm run dev --workspace=api
NEXT_PUBLIC_API_URL=http://127.0.0.1:4000/api
